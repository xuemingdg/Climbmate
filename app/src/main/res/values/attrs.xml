<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="CircularProgressView">
        <attr name="backWidth" format="dimension" />    <!--back width-->
        <attr name="progWidth" format="dimension" />    <!--progress width-->
        <attr name="backColor" format="color" />        <!--back color-->
        <attr name="progColor" format="color" />        <!--progress color-->
        <attr name="progStartColor" format="color" />   <!--start color-->
        <attr name="progFirstColor" format="color" />   <!--end color-->
        <attr name="progress" format="integer" />       <!--progress-->
    </declare-styleable>
</resources>
